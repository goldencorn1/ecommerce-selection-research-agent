# C1 评测与质量证明报告

日期：2026-08-17
模式：Mock 离线评测
数据集：`data/evaluation/ecommerce_cases.jsonl`，共 50 条
数据集版本：`ecommerce-eval-v1`

## 一、执行结论

C1 的自动化评测基线已完成：50 条样例全部运行，成功率 100%，预期降级样例通过率 100%。评测结果、汇总、配置、数据集哈希和人工抽检队列均已保存。

该结果证明的是固定数据集下的结构化报告、证据门禁和降级链路，不代表真实市场准确率、真实模型效果或商业决策成功率。

## 二、总体指标

| 指标 | 结果 |
|---|---:|
| 总样例数 | 50 |
| 实际测量样例数 | 50 |
| 成功率 | 100% |
| 预期降级样例 | 8 |
| 降级通过率 | 100% |
| Judge 平均分 | 79.60/100 |
| 平均延迟 | 14.70 ms |
| P50 延迟 | 7.73 ms |
| P95 延迟 | 9.47 ms |
| P99 延迟 | 354.96 ms |
| 平均 warning 数 | 0.52 |
| 外部请求数 | 0 |
| Mock 成本 | 0 USD |
| 模型 token | 0 |

P99 明显高于 P50/P95，说明 Mock 路径整体很快，但存在少量初始化或环境抖动样本；这不是业务错误，后续可在固定进程预热和多轮运行后进一步观察。

## 三、自动指标

| 指标 | 平均分 | 通过率 |
|---|---:|---:|
| 报告完整性 | 0.9933 | 98% |
| 品类相关性 | 0.9700 | 98% |
| 证据覆盖率 | 0.9753 | 96% |
| 评分有效性 | 1.0000 | 100% |
| 降级 warning 质量 | 1.0000 | 100% |
| 结构化输出有效性 | 1.0000 | 100% |

预期降级模块不会再被错误地当作普通报告缺失：当评测标注市场、竞品、用户或机会风险模块预期失败时，完整性指标会豁免对应板块，但仍会检查未降级的公共结构和证据边界。

当前仍有两个应保留的低分案例：

- `ec-027`：客户和机会模块降级，同时证据数量低于最低要求；
- `ec-028`：四个研究模块全部降级，无法形成证据和完整品类相关性。

这两个案例属于质量门禁应暴露的边界，不应通过修改指标把问题隐藏掉。

## 四、Judge 维度

| 维度 | 平均分 |
|---|---:|
| 市场概况 | 58.80 |
| 风险与进入壁垒 | 70.50 |
| 商业边界 | 73.96 |
| 竞品分析 | 73.63 |
| 证据质量 | 90.84 |
| 客群匹配 | 94.00 |
| 价格带分析 | 96.00 |

下一轮质量优化应优先关注市场概况、风险识别和竞品分析，而不是继续优化已经较高的价格字段和客群匹配字段。

## 五、评测产物

目录：`artifacts/evaluation/`

- `ecommerce-eval-v1.json`：50 条逐案例原始结果；
- `ecommerce-eval-v1-summary.json`：带配置的汇总结果；
- `ecommerce-eval-v1-config.json`：数据集哈希、运行时间、模式和成本配置；
- `ecommerce-eval-v1-report.md`：可展示的 Markdown 报告；
- `ecommerce-eval-v1-human-review.jsonl`：10 条人工抽检队列；
- `ecommerce-eval-v1.json` 中的 `judge.auto_metrics`：逐案例自动指标和失败详情。

可重复运行：

```powershell
.venv\Scripts\python.exe scripts\run_ecommerce_eval.py
```

## 六、人工抽检边界

已生成覆盖正常、降级、低证据、价格缺失、私有知识命中/未命中和商业核验不完整场景的 10 条抽检队列。当前队列状态为 `pending`，没有虚构人工分数；需要真实评审人填写五大板块评分、证据支持关系、降级诚实性和备注后，才能形成正式人工校准结果。

## 七、测试结果

- C1 评测相关测试：14 passed；
- Ruff：通过；
- `git diff --check`：通过；
- 全量 Mock 评测：50/50 成功；
- 已知环境提示：直接运行小范围 pytest 会触发项目全局 coverage fail-under=25% 的门槛，这是覆盖率配置行为，不是测试断言失败；使用 `--no-cov` 后 14 项全部通过。

## 八、下一步

1. 由评审人完成 10 条人工抽检并记录校准差异；
2. 进入 C2，使用用户具备合法权限的真实搜索 API 做单品类验证；
3. 使用用户配置的 DeepSeek 或其他模型完成低成本真实调用，记录真实 token、成本和稳定性；
4. 根据市场概况、风险和竞品维度的低分结果优化报告生成逻辑。
